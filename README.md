# Bus Ticket Booking System (Frontend)

Frontend-only DBMS project built with React, Vite, and Tailwind CSS.

## Run Locally

```bash
npm install
npm run dev
```

Open the URL shown in terminal (usually http://localhost:3000).

## Build

```bash
npm run build
```
